"""PDF helpers placeholder."""

from __future__ import annotations

from pathlib import Path


def extract_text_from_pdf(pdf_path: Path) -> str:
    """TODO: implement PDF extraction with page-level anchors."""
    raise NotImplementedError(f"TODO: parse PDF text and spans from {pdf_path}")
